* for(a=1;a<=n;a++)
  {
   if(a==Z[j])
  {
    for (b=1;b<=n;b++)
    {  
      if (b==Z[j])
        {
          d= ((a*b )% n);
          X[a][b]= d; 
         printf("the array is X[%d][%d] is %d\n" , a,b,X[a][b]);
         }
       }
     }
  }

}
  } */

/*printf("enter  no whose inverse is needed");
scanf("%d",&e);

for(f=1;f<=n;f++)
  {
   for(g=1;g<=n;g++)
   {
    if (X[f][g] == X[a][b])
    {
      h= ((e*a) % n );
       if (h == 1)
         {
         printf("Inverse of %d is %d \n",e,h);
          }
           }
           }
}
}
  } */

/*printf("enter  no whose inverse is needed");
scanf("%d",&e);

for(f=1;f<=n;f++)
  {
   for(g=1;g<=n;g++)
   {
    if (X[f][g] == X[a][b])
    {
      h= ((e*a) % n );
       if (h == 1)
         {
         printf("Inverse of %d is %d \n",e,h);
          }
           }
           }
           }

}
 }
*/


/* void Zn()
{
int i,h,n,j,a,b,c,d,e,f,g;
int  Z[n];
int X[n][n];
for(i=1;i<=n;i++)
{
 if (gcd(i,n)==1){
   for(j=1;j<=n;j++)
   {
     Z[j]=i;
    printf("ARRAY FOR Zn Z[%d] IS %d \n", j,i );
}
}

for(a=1;a<=n;a++)
  {
   if(a==Z[j])
  {
    for (b=1;b<=n;b++)
    {  
      if (b==Z[j])
        {
          d= ((a*b )% n);
          X[a][b]= d; 
         printf("the array is X[%d][%d] is %d\n" , a,b,X[a][b]);
         }
       }
     }
  }

printf("enter  no whose inverse is needed");
scanf("%d",&e);

for(f=1;f<=n;f++)
  {
   for(g=1;g<=n;g++)
   {
    if (X[f][g] == X[a][b])
{
      h= ((e*a) % n );
       if (h == 1)
         {
         printf("Inverse of %d is %d \n",e,h);
          }
           }
           }
           }

}
 }
*/

/* void Zn()
{
int i,h,n,j,a,b,c,d,e,f,g;
int  Z[n];
int X[n][n];
for(i=1;i<=n;i++)
{
 if (gcd(i,n)==1){
   for(j=1;j<=n;j++)
   {
     Z[j]=i;
    printf("ARRAY FOR Zn Z[%d] IS %d \n", j,i );
  }
}

for(a=1;a<=n;a++)
  {
   if(a==Z[j])
  {
    for (b=1;b<=n;b++)
    {  
      if (b==Z[j])
        {
          d= ((a*b )% n);
          X[a][b]= d; 
         printf("the array is X[%d][%d] is %d\n" , a,b,X[a][b]);
         }
       }
     }
  }

printf("enter  no whose inverse is needed");
scanf("%d",&e);
or(f=1;f<=n;f++)
  {
   for(g=1;g<=n;g++)
   {
    if (X[f][g] == X[a][b])
    {
      h= ((e*a) % n );
       if (h == 1)
         {
         printf("Inverse of %d is %d \n",e,h);
          }
           }
           }
           }
}
}
*/

